# LipSee Emotion Classification API 의존성
# FastAPI 기반 감정 분류 서비스

# 웹 프레임워크
fastapi==0.104.1
uvicorn[standard]==0.24.0

# 데이터 처리 및 AI/ML
numpy==1.24.3
onnxruntime==1.13.1

tokenizers==0.15.0

# 데이터 검증 및 설정
pydantic==2.5.0

# 유틸리티
python-multipart==0.0.6

# 개발 및 테스트 (선택사항)
pytest==7.4.3
pytest-asyncio==0.21.1
httpx==0.25.2

# 프로덕션 배포 (선택사항)
gunicorn==21.2.0

# 모델 파일 자동 다운로드
# GitHub Releases에서 urllib.request로 자동 다운로드
# ONNX_ZIP_URL 환경변수로 설정된 베이스 URL에서 개별 파일 다운로드
